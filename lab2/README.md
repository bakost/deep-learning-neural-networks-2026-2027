# level-spacing — свойства случайных матриц

Лабораторная работа №2. Пакет генерирует ансамбли случайных симметричных
матриц (гауссов ортогональный ансамбль и матрицы из ±1), находит расстояния
между фиксированной парой соседних собственных чисел, нормирует их и
сравнивает с догадкой Вигнера $\rho(s) = \frac{\pi s}{2} e^{-\pi s^2/4}$.

**Отчёт с результатами и выводами — [REPORT.md](REPORT.md).**

```python
from level_spacing import run_study

study = run_study("goe", n=16, size=100_000, seed=2026)   # пункты 1–4
study.k                          # 8 — центральная пара (λ8, λ9)
study.normalized.mean()          # 1.0
study.compare("wigner")          # D_KS, χ², L1 до формулы (15)  — пункт 8
study.compare("goe-limit")       # то же до точного закона GOE при n → ∞
```

## Установка

Нужен Python ≥ 3.9, NumPy, SciPy и Matplotlib.

```bash
python3 -m venv .venv
.venv/bin/pip install -e ".[test]"
```

Без установки достаточно добавить `src` в `PYTHONPATH`:

```bash
PYTHONPATH=src python -m level_spacing run
```

На macOS 26 (Apple M1) колёсико SciPy 1.15.3 не загружается (ошибка
`__thread_bss` в dyld). SciPy 1.14.1 работает:
`pip install "scipy==1.14.1"`.

## Командная строка

```bash
python -m level_spacing run                                  # GOE: 2×2, 4×4, 16×16 по 100 000 матриц
python -m level_spacing run -e pm1 -n 2 4 16 --plot pm1.png  # матрицы из ±1 + гистограммы в файл
python -m level_spacing run -n 16 -k 1                       # другая пара уровней (λ1, λ2)
python -m level_spacing run -n 4 -m 1000 -b jacobi           # собственные числа методом Якоби из ЛР1
python -m level_spacing exact -n 4 -c sum                    # точный закон для 4×4 из ±1 (перебор)
python -m level_spacing report                               # все рисунки и таблицы отчёта (≈ 50 с)
python -m level_spacing list                                 # список ансамблей
```

Пример вывода:

```
Ансамбль: goe — N(0,1), H = A + Aᵀ (GOE); M = 100000, зерно 2026
  n     пара  <s> до норм.  дисперсия  D_KS(Вигнер)        p  D_KS(GOE∞)   P(s=0)
---------------------------------------------------------------------------------
  2    (1,2)        3.5341     0.2737        0.0026    0.199      0.0070   0.0000
  4    (2,3)        2.3817     0.2812        0.0053   <0.001      0.0030   0.0000
 16    (8,9)        1.1255     0.2868        0.0082   <0.001      0.0018   0.0000
Догадка Вигнера: дисперсия 0.2732; точный закон GOE (n → ∞): 0.2855.
```

Бэкенд `jacobi` использует пакет `jacobi_eigen` из `../lab1`. Нужно либо
`pip install -e ../lab1`, либо `PYTHONPATH=src:../lab1/src`.

## Ансамбли

| Имя | Построение | Где в задании |
|---|---|---|
| `goe` | $A_{ij} \sim \mathcal N(0,1)$, $H = A + A^T$ (раздел 1.3 методички) | пункты 1–8 |
| `pm1` | симметричная матрица, $H_{ij} = \pm 1$ | пункт 9, трактовка (а) |
| `pm1-sum` | $A_{ij} = \pm 1$, $H = A + A^T$ | пункт 9, трактовка (б) |
| `normal-mirror` | симметричная матрица, $H_{ij} \sim \mathcal N(0,1)$ | сравнение: зачем $A + A^T$ |

## Тесты

```bash
python -m pytest                                       # 206 тестов, ≈ 12 с
PYTHONPATH=src python -m pytest --doctest-modules src  # 26 примеров из докстрингов
ruff check src tests
```

| Файл | Что проверяет |
|---|---|
| `test_ensembles.py` | форма, симметрия, распределение элементов (дисперсии 4 и 2 у $A + A^T$), воспроизводимость |
| `test_spectrum.py` | собственные числа (формулы (4), (5)), нумерация пар, нормировка, бэкенд Якоби |
| `test_distributions.py` | формулы (13)–(15), нормировка и моменты законов, точный закон GOE (дисперсия 0.2855, наклон $\pi^2/6$) |
| `test_exact.py` | полный перебор ±1, совпадение аналитики пункта 10 с перебором и моделированием |
| `test_stats.py` | критерии согласия, поправка Лиллиефорса, оценка показателя отталкивания |
| `test_experiments.py` | главные выводы отчёта: 2×2 = Вигнер, 16×16 ≠ Вигнер, 16×16 = GOE$_\infty$, универсальность |
| `test_cli.py`, `test_plots.py` | командная строка и рисунки |

Статистические тесты используют фиксированные зёрна и поэтому
детерминированы.

## Структура

```
lab2/
├── REPORT.md                  отчёт (пункт 11)
├── README.md                  этот файл
├── pyproject.toml
├── src/level_spacing/
│   ├── ensembles.py           ансамбли матриц                        — пункты 1, 9
│   ├── spectrum.py            собственные числа, разности, нормировка — пункты 2–4
│   ├── distributions.py       догадка Вигнера, (13), точный GOE, 2×2 из ±1 — пункты 7, 10
│   ├── exact.py               точные законы для ±1 полным перебором   — пункт 10
│   ├── stats.py               гистограммы, KS, χ², L1, показатель отталкивания — пункты 5, 6, 8
│   ├── experiments.py         сценарии эксперимента
│   ├── plots.py               рисунки
│   ├── report.py              пересчёт всего отчёта
│   └── __main__.py            командная строка
├── tests/                     206 тестов
├── figures/                   рисунки отчёта (генерируются)
└── results/                   summary.json и tables.md (генерируются)
```

## Что сделано по пунктам задания

| Пункт | Где |
|---|---|
| 1. Ансамбли 2×2, 4×4, 16×16, ≥ 1000 матриц | `ensembles.py`; в отчёте $M = 10^3$, $10^5$, $10^6$ |
| 2. Собственные числа по возрастанию | `spectrum.eigenvalues` (LAPACK; проверено методом Якоби из ЛР1) |
| 3. Разности соседних (фиксированная пара) | `spectrum.pair_spacings`, центральная пара $k = n/2$; выбор обоснован в отчёте, раздел 3.4 |
| 4. Нормировка на среднее 1 | `spectrum.normalize` |
| 5. Гистограмма | рисунок 2 |
| 6. Поведение около нуля | отчёт, раздел 3.2, рисунок 3 |
| 7. График (15) на том же холсте | рисунки 2, 6, 7 |
| 8. Близость графика к гистограмме | отчёт, раздел 3.3: KS, χ², L1, остатки (рис. 4), зависимость от $M$ (рис. 5), сравнение с точным законом GOE |
| 9. Матрицы из ±1 | отчёт, раздел 4, рисунки 6–7, обе трактовки |
| 10. Аналитика для 2×2 из ±1 | отчёт, раздел 5; `distributions.pm1_2x2_distribution`; проверено перебором |
| 11. Комментарии и отчёт | [REPORT.md](REPORT.md) |
